services:
  backend:
    build: .
    env_file:
      - .env
    volumes:
      - ./app:/app/app

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
